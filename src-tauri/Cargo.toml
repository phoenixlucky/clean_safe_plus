[package]
name = "clean-safe-plus"
version = "2.0.0"
description = "A safe Windows C drive cleanup client"
authors = ["Clean Safe Plus"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.5", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2.11", features = ["image-png"] }
